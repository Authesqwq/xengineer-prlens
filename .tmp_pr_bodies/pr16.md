## 功能描述
将侧栏改为工作区，新增三种分析模式、会话级历史记录、导出报告和清空历史。快速模式仅 Summary，标准模式 Summary+Risk，完整模式全部。

## 实现思路
通过 mode key 控制分析深度，st.session_state 管理模式和历史。历史按 URL、语言和模式去重，最多 10 条。

## 测试方式
- [x] pytest -q
- [x] 手动验证三种模式、历史恢复、导出报告
